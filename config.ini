[logging]
# Directory where daily CSV log files are stored
log_dir = logs
# Sensor reading and logging interval in seconds (300 = 5 minutes)
interval_seconds = 2

[display]
# Hour of day (0-23) when the OLED screen turns on
daylight_start = 8
# Hour of day (0-23) when the OLED screen turns off
daylight_end = 22

[web]
host = 0.0.0.0
port = 80
